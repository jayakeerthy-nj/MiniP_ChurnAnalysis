# Frontend environment variables
# Copy to frontend/.env.local for local development
#
# BACKEND_URL is used by Next.js at build time to configure the /api/* rewrite proxy.
# For local development, this defaults to http://127.0.0.1:5000 if not set.
# For Docker deployments, set this to the internal Docker service name.
BACKEND_URL=http://localhost:5000
